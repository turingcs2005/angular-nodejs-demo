import { WiggleDirective } from './wiggle.directive';

describe('WiggleDirective', () => {
  it('should create an instance', () => {
    const directive = new WiggleDirective();
    expect(directive).toBeTruthy();
  });
});
